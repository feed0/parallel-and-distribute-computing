# Baixar o pip3
curl -O https://bootstrap.pypa.io/get-pip.py

# Instalar o pip3 no root
python3 get-pip.py

# Instalar flask
pip3 install flask

# Install git
sudo yum install git -y

# Clone repo
git clone https://github.com/feed0/parallel-and-distribute-computing.git

# Rodar aplicação (app.py é o nome do arquivo)
python3 app.py